# Marketplace Scripts

- `validate-marketplace.mjs` validates the static catalog shape.
- `build-site.mjs` generates the GitHub Pages site into `dist/`.
- `verify-test-layout.mjs` validates tracked repository test paths; it is repository tooling and is not included in the published marketplace runtime.
